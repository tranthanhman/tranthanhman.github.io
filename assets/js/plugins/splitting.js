(function () {
    // Init default
    Splitting();
    // Customize effect for loading screen
    Splitting({
        target: ".loading-overlay",
        by: 'cells',
        image: true
    });
})();
